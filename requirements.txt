click==8.1.7           # Upgraded from 7.1.2
Flask==2.3.2           # Remains the same
gunicorn==20.1.0       # Updated to latest version
itsdangerous==2.1.2    # Updated to latest compatible version
Jinja2==3.1.2          # Upgraded from 2.11.3 for Flask compatibility
MarkupSafe==2.1.3      # Updated to latest compatible version
Werkzeug==2.3.7        # Upgraded from 1.0.1 for Flask compatibility
requests==2.31.0       # Remains the same
pypdf==3.8.1           # Remains the same

